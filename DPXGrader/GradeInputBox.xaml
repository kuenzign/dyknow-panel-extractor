<UserControl x:Class="DPXGrader.GradeInputBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="160" Width="220">
    <Grid>
        <Canvas Name="theRootCanvas">
            <Border
                Background="White"
                BorderBrush="Black"
                BorderThickness="2"
                Height="100"
                Width="200"
                Canvas.Top="10"
                Canvas.Left="10">
                <InkCanvas Name="theInkCanvas"></InkCanvas>
            </Border>
            <TextBox Name="textBox"
                Height="25"
                Width="120"
                Canvas.Top="125"
                Canvas.Left="10">
            </TextBox>
            <Button
                Height="25"
                Width="75"
                Canvas.Top="125"
                Canvas.Left="135"
                Click="ButtonClick">Recognize</Button>
        </Canvas>
    </Grid>
</UserControl>
